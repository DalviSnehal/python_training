# Ask the user for an integer between 1 and 10 (inclusive).
# If the number they give is outside of the specified range,
# raise a ValueError and inform them that their choice was invalid.


numbers = [1, 2, 3, 4, 5]
print(numbers[100])
raise " ValueError and inform them that their choice was invalid."